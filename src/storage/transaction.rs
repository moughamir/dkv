//! Transaction support for the `SQLite` backend.
//!
//! Helpers for running `ingest` as one atomic transaction, including content
//! file staging under the vault `tmp` directory. Implemented in a later
//! milestone.
